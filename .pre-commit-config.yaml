# DO NOT EDIT. Generated with:
#
#    devctl
#
#    https://github.com/giantswarm/devctl/blob/6dc24309a46587e01ddebe9fd12fb961e108b873/pkg/gen/input/precommit/internal/file/pre-commit-config.yaml.template
#
minimum_pre_commit_version: '2.17'
default_install_hook_types: [pre-commit, commit-msg]
repos:
  # base hooks
  #
  # helm/<chart>/charts/ holds vendored upstream subcharts (vendir-synced).
  # Those files are not repo-authored: the auto-fixing hooks would rewrite
  # them on every vendir bump and the next vendir sync would revert the fix,
  # so the whitespace/shebang/private-key hooks skip the vendored subtree.
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-merge-conflict
      - id: check-shebang-scripts-are-executable
        exclude: "^helm/[^/]+/charts/.*"
      - id: detect-private-key
        # Upstream charts document key formats with placeholder PEM headers
        # (redis-ha's restore.ssh.key example) that the hook cannot tell from
        # a key; gitleaks stays the scanner of record for the vendored subtree.
        exclude: "^helm/[^/]+/charts/.*"
      - id: end-of-file-fixer
        exclude: "(.*testdata/.*|^\\.yarn/.*|^helm/[^/]+/charts/.*)"
      - id: mixed-line-ending
        exclude: "^helm/[^/]+/charts/.*"
      - id: trailing-whitespace
        exclude: "(.*testdata/.*|^\\.yarn/.*|^helm/[^/]+/charts/.*)"

  - repo: https://github.com/compilerla/conventional-pre-commit
    rev: v4.4.0
    hooks:
      - id: conventional-pre-commit
        stages: [commit-msg]
        args: [--strict]

  # Go hooks
  - repo: https://github.com/dnephin/pre-commit-golang
    rev: v0.5.1
    hooks:
      - id: go-fmt
      - id: go-mod-tidy
      - id: golangci-lint
        args:
          - -E=gosec
          - -E=goconst
          - -E=govet
          # timeout is needed for CI
          - --timeout=300s
          # List all issues found
          - --max-same-issues=0
          - --max-issues-per-linter=0
      - id: go-imports
        args: [ -local, github.com/giantswarm/vm-manager ]

  # Helm chart schema for vm-manager: generate -> fix $ref -> normalize, as ONE hook.
  #
  # This single local hook deliberately runs the whole schema pipeline. pre-commit does
  # NOT feed hooks into each other: it runs each hook independently against the file on
  # disk and fails the run if any hook modified it. So for a multi-fixer setup to pass,
  # all fixers must agree on ONE resting format. Splitting these steps into separate
  # hooks gives them rival resting formats and `pre-commit run -a` can then never
  # converge -- reruns just ping-pong between formats and one hook always fails:
  #
  #   * `helm schema` regenerates the schema from values.yaml wholesale, in the
  #     generator's own key ordering, and does not preserve existing formatting.
  #   * `schemalint normalize` rewrites it into schemalint's canonical key ordering
  #     (and unicode escaping), which is NOT the generator's ordering.
  #   * the $ref fix below rewrites the generator's output again.
  #
  # Chained here, `schemalint normalize` is always the LAST writer, so the normalized
  # form is the single resting format and the committed file is a fixed point -- the
  # hook is a no-op on an already-processed schema and the run is clean. Read-only
  # `schemalint-verify` then checks that resting format (it never rewrites, so it is not
  # part of the conflict). See giantswarm/giantswarm#37267.
  #
  # The `$ref` step works around a generator bug: with `noAdditionalProperties: true` it
  # emits `additionalProperties: false` as a sibling of every bare `$ref`. Per JSON Schema
  # 2020-12, `additionalProperties` only sees sibling `properties`/`patternProperties`
  # -- NOT properties pulled in through `$ref` -- so it wrongly rejects every field the
  # referenced schema defines (e.g. `helm template` fails with "additional properties
  # 'podAntiAffinity' not allowed" for a k8s Affinity). The correct 2020-12 keyword is
  # `unevaluatedProperties`, which DOES consider $ref-evaluated properties, but the tool
  # cannot emit it and does not drop the bogus `additionalProperties` when annotated. So
  # for every object that has both `$ref` and `additionalProperties: false` we drop
  # `additionalProperties` and set `unevaluatedProperties: false`.
  # Upstream bug: https://github.com/losisin/helm-values-schema-json/issues/317
  #
  # This replaces both the external `losisin/helm-values-schema-json` hook and the
  # separate `schemalint-normalize` hook. BOTH binaries are installed and pinned by the
  # hook itself (`additional_dependencies`, hence `language: golang`), so the pipeline
  # runs the same versions on a developer machine and in CI.
  #
  # `helm-values-schema-json` is the module behind the `helm schema` plugin -- the very
  # same binary, called directly, so nothing here depends on a helm plugin any more. It
  # used to: the hook called a bare `helm schema`, resolved from whatever the developer
  # had in their global helm plugin dir, while CI pinned a version in a different file.
  # The old guard only checked that *a* plugin existed, never which version, so a wrong
  # version printed "Passed" locally while rewriting the committed schema (v2.6.0 alone
  # strips 41 lines from hello-world-app's) and CI then rejected it.
  - repo: local
    hooks:
      - id: helm-schema-vm-manager
        name: "generate + fix + normalize helm/vm-manager/values.schema.json"
        language: golang
        additional_dependencies: ['github.com/giantswarm/schemalint/v2@v2.6.3', 'github.com/losisin/helm-values-schema-json/v2@v2.6.0']
        files: ^helm/vm-manager/(values\.yaml|\.schema\.yaml|values\.schema\.json|zz_generated\.app-platform\.values\.yaml)$
        pass_filenames: false
        require_serial: true
        entry: sh -c
        args:
          - 'helm-values-schema-json --config helm/vm-manager/.schema.yaml && python3 -c ''import json,sys; h=lambda o: {**{k:v for k,v in o.items() if k!="additionalProperties"},"unevaluatedProperties":False} if ("$ref" in o and o.get("additionalProperties") is False) else o; p=sys.argv[1]; f=open(p,encoding="utf-8"); d=json.load(f,object_hook=h); f.close(); f=open(p,"w",encoding="utf-8"); json.dump(d,f); f.close()'' helm/vm-manager/values.schema.json && schemalint normalize helm/vm-manager/values.schema.json -o helm/vm-manager/values.schema.json --force'
  - repo: https://github.com/giantswarm/schemalint
    rev: v2.6.3
    hooks:
      - id: schemalint-verify
        pass_filenames: false
        args:
          - helm/vm-manager/values.schema.json
  - repo: https://github.com/norwoodj/helm-docs
    rev: "v1.14.2"
    hooks:
      - id: helm-docs
        args:
          - --chart-search-root=helm
          - --sort-values-order=file
