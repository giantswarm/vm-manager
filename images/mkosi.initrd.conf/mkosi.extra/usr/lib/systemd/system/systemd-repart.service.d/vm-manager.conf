# During the installer boot (phase A) the root disk is the read-only base
# image; creating var there would only fail. The target disk is partitioned by
# vm-sysinstall.service instead.
[Unit]
ConditionCredential=!vm.install-target
