# containerd reads /etc/containerd/config.toml by default; an extension cannot
# ship /etc, so point it at the configuration inside the extension.
[Service]
ExecStart=
ExecStart=/usr/bin/containerd --config /usr/lib/vm-manager/containerd.toml
