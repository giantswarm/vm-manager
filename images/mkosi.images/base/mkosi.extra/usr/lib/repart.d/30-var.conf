# Boot-time systemd-repart on the root disk: create var if it is missing (plain
# `mkosi vm` boot of the base image) or grow it when the disk was enlarged.
[Partition]
Type=var
Format=ext4
SizeMinBytes=1G
Weight=10000
