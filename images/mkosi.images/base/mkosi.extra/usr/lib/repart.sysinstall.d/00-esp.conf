# Target disk layout used by systemd-sysinstall (vm-sysinstall.service).
# A slots are block copies of the booted installer's partitions, B slots are
# empty and labelled _empty for systemd-sysupdate, var takes the rest.
[Partition]
Type=esp
Format=vfat
SizeMinBytes=512M
SizeMaxBytes=512M
