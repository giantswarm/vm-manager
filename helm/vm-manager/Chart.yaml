apiVersion: v2
name: vm-manager
description: VM provisioning service for the Agent Platform — KVM virtual machines with an instance metadata service, a vTPM with measured boot, an immutable image and attestation, exposed as REST and MCP
type: application
# Stamped at publish from the git tag (.github/workflows/publish.yml); the
# values here are placeholders a local `helm install` of the checkout uses.
version: 0.1.0
appVersion: "0.1.0"
home: https://github.com/giantswarm/vm-manager
icon: https://s.giantswarm.io/app-icons/giantswarm/1/dark.svg
sources:
  - https://github.com/giantswarm/vm-manager
keywords:
  - mcp
  - kvm
  - virtual-machines
  - attestation
  - agent-platform
maintainers:
  - name: Giant Swarm
    email: team-bumblebee@giantswarm.io
annotations:
  # Team ownership (OCI-compliant format)
  io.giantswarm.application.team: bumblebee
  # Target audience: customers (default catalog)
  io.giantswarm.application.audience: customers
  # App is managed/supported by Giant Swarm
  io.giantswarm.application.managed: "true"
