apiVersion: v1
kind: Service
metadata:
  name: {{ include "vm-manager.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vm-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vm-manager.selectorLabels" . | nindent 4 }}
