# System user for deploy/systemd/vm-manager.service. The unit adds the kvm
# group through SupplementaryGroups=, so nothing else is needed here.
#Type Name        ID  GECOS                         Home directory        Shell
u     vm-manager  -   "vm-manager VM provisioner"   /var/lib/vm-manager   -
